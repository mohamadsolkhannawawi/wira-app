# Frontend Module

This folder contains the React + Vite frontend for WIRA App.

For complete project documentation (English and Indonesian in separate files), see:

- ../README.md

For local run:

- npm install (from repository root)
- npm run dev (from repository root)
